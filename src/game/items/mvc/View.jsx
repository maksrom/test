/** View implementation in MVC pattern. */
export class View {
    /** Set model link inside view. */
    setModel(model) {
        this.model = model;
    }

    /** Animate function implementation which calls 60 times per sec. */
    animate(ctx) {}
}